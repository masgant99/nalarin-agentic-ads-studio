# Production bundle uses nginx's same-origin proxy for /api and /uploads.
# Never bake provider tokens, database URLs, or service secrets into a Vite
# build: all sensitive configuration stays in the backend container's .env.
VITE_API_URL=/api/v1
